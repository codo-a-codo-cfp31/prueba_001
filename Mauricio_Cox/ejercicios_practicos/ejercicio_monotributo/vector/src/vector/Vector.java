/**
 realizar un programa que permita cargar un vector. con 10 numeros enteross, y determinar los siguiente
 A_ cuantos son mayores a 50?
 FB_ cuantos son menores a 70 ? 
 
 * la recorrida: permitir la carga de 15 números en 2 vectores, luego realizar la suma de cada una de las posiciones de ambos, 
 * y almacenarlos en otro vector, finalmente mostrar el contenido del tercer vector, de forma amigable.
 * 
 
 **/
package vector;

/**
 *
 * @author CFP31-14
 */
public class Vector {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
    int[] num = new int[15];  
    
    for( int i=0; i<15; i++ ){
    System.out.println( "ingrese num" );    
    }
    }
    
}
